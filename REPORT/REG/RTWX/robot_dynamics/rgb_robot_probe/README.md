# rgb_robot_probe — CLOSED

文件名未改：`RTWX0RGB_*`、`RTWX0RGB1_*`。

科学问题是 \(RGB\to(q,\dot q)\)，**不是** \(RGB\to s^O\)。

```
RTWX-X0RGB* = CLOSED
              robot-state-from-RGB probe
RTWX-O0+    = NEW world_perception branch
```

不要把 O0 写成 X0RGB2。
